def includes = ['lib-common', 'lib-commonbase', 'lib-json', 'api-data-store-server-java', 'api-openbis-java',
'api-openbis-javascript', 'lib-openbis-common', 'lib-dbmigration', 'lib-authentication', 'api-data-store-server-javascript',
'server-original-data-store', 'server-application-server'
]

includes.forEach { name ->
    includeFlat(name)
    project(":${name}").projectDir = new File("../../../../../../../../../../${name}")
}